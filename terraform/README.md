# Terraform layout

`modules/` contains reusable infrastructure components. `environments/` composes them for dev, staging, and production. Remote state and AWS account-specific values are intentionally not committed.

